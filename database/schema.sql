CREATE DATABASE IF NOT EXISTS gestion_presence
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE gestion_presence;

-- Les tables applicatives sont creees par Laravel :
-- php artisan migrate --seed
